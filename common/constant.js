const ROLE = {
  USER: 'user',
};

const EXPIRE_MINUTE_REGISTER_CODE = 10; // 10 minutes

const HEADERS = {
  AUTH_TOKEN: 'auth-token',
};

module.exports = {
  ROLE,
  EXPIRE_MINUTE_REGISTER_CODE,
  HEADERS,
};
